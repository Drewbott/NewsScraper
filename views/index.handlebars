{{!-- <div class="card">
  <div class="card-header">  
      {{#each articles}}
    <li>
      <p>Title: {{title}}</p>
      <p>Link: {{link}}</p>
    </li>
  {{/each}}
</ul> --}}

 {{#each articles}}
 <div class="container-fluid">
    <div class="row">
     <div class="col-md-12">
<div class="card text-center">
  <div class="card-header">
    
  </div>
  <div class="card-body">
    <h5 class="card-title">{{title}}</h5>
    <p class="card-text"> <a href="{{link}}">{{link}}</a></p>
    <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModal">
  Make Note
</button>

  </div>
</div>
</div>
</div>
</div>


{{/each}}

{{!-- modal --}}
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Notes</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form id="userInput">
        <div class="form-group">
    <label for="exampleFormControlTextarea1">Type your thoughts</label>
    <textarea class="form-control" id="comments" rows="3"></textarea>
  </div>
</form>
      </div>
      <div class="modal-footer">
        {{!-- <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button> --}}
        <button type="button" id="btnNote" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>

<script>

$("#btnNote").on("click", function(){
  $("#exampleModal").modal('hide')
  console.log("Howdy")
})

$("#userInput").on("submit", function(event){
     event.preventDefault();
     console.log("Howdy")
     var comment = $("#comments").val().trim()
     
     var note= {
       title: title,
       body: comment
     }
     
     $.post("/articles/:id", note, function(){
        console.log("Howdy")
     })
});

</script>


